name: Lazy Update

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  lazy-update:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install neovim
      - run: nvim --headless "+Lazy! sync" +qa
